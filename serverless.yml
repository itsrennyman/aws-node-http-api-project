service: aws-node-http-api-project
frameworkVersion: "3"

provider:
  name: aws
  stage: dev
  region: eu-west-1
  runtime: nodejs14.x

functions:
  hello:
    handler: handler.hello
    events:
      - httpApi:
          path: /
          method: get
